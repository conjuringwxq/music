import React from 'react';
import { ProfileItemProps } from '@/pages/profile';

export const ProfileSimilar: React.FC<ProfileItemProps> = (props) => {
  const { data, loading } = props;

  return <>相似歌手</>;
};
