import React from 'react';
import styled from 'styled-components';
import { SearchItemProps } from './index';

const SearchMv: React.FC<SearchItemProps> = (props) => {
  const { loading, data } = props;

  return <>搜索MV</>;
};

export default SearchMv;
