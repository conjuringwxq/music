import React from 'react';
import styled from 'styled-components';
import { SearchItemProps } from './index';

const SearchVideo: React.FC<SearchItemProps> = (props) => {
  const { dispatch, loading, data } = props;

  return <>搜索视频</>;
};

export default SearchVideo;
